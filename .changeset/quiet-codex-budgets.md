---
"@howaboua/pi-codex-conversion": patch
---

Keep Codex context-budget adjustment idempotent across Pi session replacement and extension reloads so the displayed context window and auto-compaction threshold do not shrink repeatedly.
